{
  "name": "proxyist",
  "description": "Filesystem REST API which supports multiple backend plugins",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "version": "version=$(jq -r .version lerna.json); sed -i -e 's/\\(\"@paradisec\\/[^\"]*\": \"^\\)[0-9.]*/\\1'$version'/' docker/package.json && yarn install && git add docker/package.json docker/yarn.lock"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.4"
  }
}
